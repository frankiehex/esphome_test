esphome:
  name: ws2812-led-effects
  platform: ESP32
  board: esp32dev
  min_version: 2024.1.0
  on_boot:
    priority: 600
    then:
      - logger.log:
          format: "WS2812 LED Effects v1.0.1 started - System ready"
          level: INFO

# WiFi configuration
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  fast_connect: true
  power_save_mode: light
  ap:
    ssid: "ws2812-fallback"
    password: "12345678"

# Enable logging
logger:
  level: DEBUG
  logs:
    component: DEBUG

# Enable Home Assistant integration
api:
  encryption:
    key: !secret api_key

# Over-the-Air Updates
ota:
  platform: esphome
  password: !secret ota_password

# Web Server for control interface
web_server:
  port: 80
  auth:
    username: admin
    password: !secret web_password

# NeoPixel strip configuration for WS2812 LED
light:
  - platform: fastled_clockless
    chipset: WS2812B
    pin: GPIO4
    num_leds: 30
    rgb_order: GRB
    name: "WS2812 LED Strip"
    default_transition_length: 500ms
    effects:
      - pulse:
          name: "Pulse"
          transition_length: 250ms
          update_interval: 250ms
          min_brightness: 50%
          max_brightness: 100%
      - random:
          name: "Random"
          transition_length: 5s
          update_interval: 10s
      - strobe:
          name: "Strobe"
          colors:
            - state: true
              red: 100%
              green: 0%
              blue: 0%
              duration: 300ms
            - state: false
              duration: 300ms

# GPIO Configuration Reference
# GPIO4  - WS2812 Data (default) - Recommended for stability
# GPIO23 - Alternative pin (with level shifter recommended for 3.3V)
# GPIO5  - Alternative pin (with level shifter recommended for 3.3V)
# GPIO25 - Alternative pin (PWM capable, with level shifter recommended)
#
# Recommendations:
# - Use 3.3V to 5V level shifter for reliable 5V WS2812 communication
# - Ensure proper power supply (5V 1-2A minimum for 30 LEDs)
# - Add 100-470µF capacitor across 5V and GND near LED strip
# - Add 1K resistor in series with data line for protection

packages:
  version: !include version.yaml
